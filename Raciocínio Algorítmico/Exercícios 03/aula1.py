# Leia uma temperatura qualquer e informe se está quente (maior que 25)

temperatura = float(input("Digite a temperatura: "))
if temperatura > 25:
    print("Está quente.")
else:    print("Está frio.")
